diff --git a/axi_spi_slave.sv b/axi_spi_slave.sv
--- a/axi_spi_slave.sv
+++ b/axi_spi_slave.sv
@@ -128,7 +128,7 @@ module axi_spi_slave
 
     logic [15:0] wrap_length;
 
-    spi_slave_rx u_rxreg
+    pulp_spi_slave_rx u_rxreg
     (
         .sclk           ( spi_sclk       ),
         .cs             ( spi_cs         ),
@@ -143,7 +143,7 @@ module axi_spi_slave
         .data_ready     ( rx_data_valid  )
     );
 
-    spi_slave_tx u_txreg
+    pulp_spi_slave_tx u_txreg
     (
         .test_mode      ( test_mode      ),
         .sclk           ( spi_sclk       ),
@@ -160,7 +160,7 @@ module axi_spi_slave
         .done           ( tx_done        )
     );
 
-    spi_slave_controller
+    pulp_spi_slave_controller
     #(
         .DUMMY_CYCLES ( DUMMY_CYCLES )
     )
@@ -192,7 +192,7 @@ module axi_spi_slave
         .wrap_length        ( wrap_length        )
     );
 
-    spi_slave_dc_fifo
+    pulp_spi_slave_dc_fifo
     #(
         .DATA_WIDTH   ( 32 ),
         .BUFFER_DEPTH ( 8  )
@@ -211,7 +211,7 @@ module axi_spi_slave
         .ready_b ( fifo_data_rx_ready )
     );
 
-    spi_slave_dc_fifo
+    pulp_spi_slave_dc_fifo
     #(
         .DATA_WIDTH   ( 32 ),
         .BUFFER_DEPTH ( 8  )
@@ -298,7 +298,7 @@ module axi_spi_slave
         .wrap_length          ( wrap_length                  )
     );
 
-    spi_slave_syncro
+    pulp_spi_slave_syncro
     #(
         .AXI_ADDR_WIDTH ( AXI_ADDR_WIDTH )
     )
diff --git a/spi_slave_cmd_parser.sv b/spi_slave_cmd_parser.sv
--- a/spi_slave_cmd_parser.sv
+++ b/spi_slave_cmd_parser.sv
@@ -8,7 +8,7 @@
 // CONDITIONS OF ANY KIND, either express or implied. See the License for the
 // specific language governing permissions and limitations under the License.
 
-module spi_slave_cmd_parser(
+module pulp_spi_slave_cmd_parser(
       input  logic [7:0] cmd,
       output logic       get_addr,
       output logic       get_mode,
diff --git a/spi_slave_controller.sv b/spi_slave_controller.sv
--- a/spi_slave_controller.sv
+++ b/spi_slave_controller.sv
@@ -12,7 +12,7 @@
 `define SPI_QUAD_TX 2'b01
 `define SPI_QUAD_RX 2'b10
 
-module spi_slave_controller
+module pulp_spi_slave_controller
     #(
     parameter DUMMY_CYCLES = 32
     )
@@ -84,7 +84,7 @@ module spi_slave_controller
 
   assign command = decode_cmd_comb ? rx_data : cmd_reg;
 
-  spi_slave_cmd_parser u_cmd_parser(
+  pulp_spi_slave_cmd_parser u_cmd_parser(
       .cmd(command),
       .get_addr(get_addr),
       .get_mode(get_mode),
@@ -97,7 +97,7 @@ module spi_slave_controller
       .reg_sel(reg_sel)
       );
 
-  spi_slave_regs #(
+  pulp_spi_slave_regs #(
       .REG_SIZE(REG_SIZE)
     ) u_spiregs(
       .sclk(sclk),
diff --git a/spi_slave_dc_fifo.sv b/spi_slave_dc_fifo.sv
--- a/spi_slave_dc_fifo.sv
+++ b/spi_slave_dc_fifo.sv
@@ -8,7 +8,7 @@
 // CONDITIONS OF ANY KIND, either express or implied. See the License for the
 // specific language governing permissions and limitations under the License.
 
-module spi_slave_dc_fifo #(
+module pulp_spi_slave_dc_fifo #(
     parameter DATA_WIDTH = 32,
       parameter BUFFER_DEPTH = 8
       )
diff --git a/spi_slave_regs.sv b/spi_slave_regs.sv
--- a/spi_slave_regs.sv
+++ b/spi_slave_regs.sv
@@ -8,7 +8,7 @@
 // CONDITIONS OF ANY KIND, either express or implied. See the License for the
 // specific language governing permissions and limitations under the License.
 
-module spi_slave_regs #(
+module pulp_spi_slave_regs #(
   parameter REG_SIZE = 8
 ) (
   input  logic                sclk,
diff --git a/spi_slave_rx.sv b/spi_slave_rx.sv
--- a/spi_slave_rx.sv
+++ b/spi_slave_rx.sv
@@ -8,7 +8,7 @@
 // CONDITIONS OF ANY KIND, either express or implied. See the License for the
 // specific language governing permissions and limitations under the License.
 
-module spi_slave_rx
+module pulp_spi_slave_rx
 (
     input  logic        sclk,
     input  logic        cs,
diff --git a/spi_slave_syncro.sv b/spi_slave_syncro.sv
--- a/spi_slave_syncro.sv
+++ b/spi_slave_syncro.sv
@@ -8,7 +8,7 @@
 // CONDITIONS OF ANY KIND, either express or implied. See the License for the
 // specific language governing permissions and limitations under the License.
 
-module spi_slave_syncro
+module pulp_spi_slave_syncro
     #(
     parameter AXI_ADDR_WIDTH = 32
     )
diff --git a/spi_slave_tx.sv b/spi_slave_tx.sv
--- a/spi_slave_tx.sv
+++ b/spi_slave_tx.sv
@@ -8,7 +8,7 @@
 // CONDITIONS OF ANY KIND, either express or implied. See the License for the
 // specific language governing permissions and limitations under the License.
 
-module spi_slave_tx
+module pulp_spi_slave_tx
 (
     input  logic        test_mode,
     input  logic        sclk,
